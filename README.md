# OLCWave

Панель управления для [OLCRTC](https://github.com/openlibrecommunity/olcrtc), интегрированная с [Remnawave](https://docs.rw/).

OLCWave хранит шаблоны конфигураций OLCRTC (`profiles`). Когда пользователь добавляет подписку, панель проверяет его через Remnawave, создаёт отдельную конфигурацию OLCRTC для каждого профиля, запускает Docker-контейнеры и возвращает готовую к импорту подписку.

Также панель отслеживает использование трафика пользователей и автоматически останавливает контейнеры при достижении установленного лимита.

## Возможности

* **Интеграция с Remnawave** - статус пользователя и срок действия подписки берутся напрямую из Remnawave. Опциональная фильтрация по squad - выдавайте подписки только участникам нужной группы
* **Профили** - переиспользуемые YAML-шаблоны конфигурации OLCRTC [Примеры профилей](https://github.com/invdevv/olcwave/tree/master/docs/сonfig_examples)
* **Автоматическая генерация конфигурации** - панель сама заполняет уникальные параметры для каждого пользователя (crypto.key, room.id итд)
* **Автоматическое создание комнат** - для некоторых провайдеров панель сама создаёт комнаты через их API. Больше не нужно вручную создавать комнаты и копировать ID. Просто укажите токен - комнаты будут создаваться автоматически для каждого пользователя. [Подробнее в docs/profiles.md](docs/profiles.md#Автоматическая-генерация-комнат)
* **Управление контейнерами** - запуск, остановка, перезапуск и удаление OLCRTC-контейнеров, просмотр логов в реальном времени и текущего `config.yaml`
* **Учёт трафика** - SOCKS-прокси внутри каждого контейнера считает переданные данные, backend собирает статистику по пользователям, контролирует лимиты и позволяет сбрасывать счётчики.
* **Routing** - маршрутизация трафика контейнеров через внешние прокси (SOCKS5, VLESS и др.) с помощью Xray-core. Направляйте трафик заблокированных сайтов через прокси, российские сайты - напрямую, остальные - за границу.
* **Подписки** - одна ссылка для каждого пользователя, которая возвращает пакет всех доступных локаций в формате, совместимом с OLCBox


## Быстрый старт

Для запуска нужны Docker, Docker Compose и работающий инстанс Remnawave.

Рекомендую устанавливать OLCWave на чистый сервер **в России**
**НЕ** рядом с remnawave

```bash
cd /opt
git clone https://github.com/user14484/olcwave.git && cd olcwave

chmod +x install.sh
./install.sh
```

После установки откройте веб-панель и войдите, используя `ADMIN_USERNAME` и `ADMIN_PASSWORD` из файла `backend/.env`.

Или установите всё вручную:

[docs/installation.md](docs/installation.md)


## Обзор архитектуры

```text
Панель администратора             Клиент пользователя

        │                                 │
        ▼                                 ▼
Frontend (React SPA)                /sub/{uuid}
        │                                 │
        └──────────────┬──────────────────┘
                       ▼
                Backend (FastAPI)
                       │
        ┌──────────────┼─────────────────────┐
        ▼              ▼                     ▼
   PostgreSQL     Remnawave API      Docker daemon
 (пользователи,  (проверка          (запускает
  профили,        пользователя,      контейнеры
  трафик,         срока действия)    OLCRTC + XrayCore)
  routing)
                                              │
                                              ▼
                                     Контейнеры OLCRTC
                                   olcwave-<tag>-<uuid>
                                              │
                                    (при enabled routing)
                                              ▼
                                      olcwave-xraycore
                                       (Xray-core)
```

* **Frontend** - приложение на React + Vite. В production собирается в статические файлы и обслуживается Caddy. Включает валидацию YAML/JSON конфигураций на клиенте.
* **Backend** - FastAPI-приложение, которое отвечает за всю логику: аутентификацию, профили, работу с Remnawave, управление контейнерами через Docker Socket, генерацию подписок, routing через Xray-core и фоновый учёт трафика.
* **PostgreSQL** - хранит локальные данные пользователей, шаблоны профилей и конфигурацию routing.
* **Docker** - backend собирает образ `olcrtc` и запускает отдельный контейнер для каждой пары "профиль - пользователь" на Docker-хосте. При включённом routing также управляет контейнером `olcwave-xraycore`.
* **OLCRTC** - основной транспорт. Каждый контейнер также запускает SOCKS-прокси, который сохраняет статистику трафика в `stats.json`. Backend периодически считывает эти данные.
* **XrayCore** (опционально) - контейнер с Xray-core, маршрутизирующий трафик OLCRTC через внешние прокси.

Подробное описание работы системы в [docs/architecture.md](docs/architecture.md).


## Документация

| Документ                                      | Описание                                                   |
| --------------------------------------------- | ---------------------------------------------------------- |
| [installation.md](docs/installation.md)       | Полная инструкция по установке на чистый Linux-сервер      |
| [configuration.md](docs/configuration.md)     | Описание всех переменных окружения и runtime-настроек      |
| [profiles.md](docs/profiles.md)               | Работа профилей, генерация конфигураций и примеры YAML     |
| [routing.md](docs/routing.md)                 | Маршрутизация трафика через Xray-core, настройка и примеры |
| [architecture.md](docs/architecture.md)       | Архитектура системы, потоки запросов, учёт трафика и Caddy |
| [development.md](docs/development.md)         | Локальная разработка и рекомендации для участников проекта |
| [troubleshooting.md](docs/troubleshooting.md) | Частые проблемы и способы их решения                       |

## Лицензия

Проект распространяется под лицензией GNU AGPL v3.

Подробнее см. файл [LICENSE](LICENSE).


## Поддержать проект

OLcWave - это open-source проект, который развивается самостоятельно.

Если вы используете проект и хотите помочь с дальнейшей разработкой, вы можете:

- поставить ⭐ на GitHub;
- сообщать о найденных проблемах;
- предлагать новые идеи;
- поддержать разработку финансово.

TON кошелек:
`UQBJ7yNz6eKZ3FPwGORmVld8p986TEc-bzX-uoRmV4_rbPsR`

Любая поддержка помогает уделять больше времени развитию проекта.

Спасибо всем, кто поддерживает OLcWave ❤️
